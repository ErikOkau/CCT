{
  "buildCommand": "npm run generate",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "outputDirectory": ".output",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
